const CATEGORIES = [
  {id:'special',name:'招牌菜',count:6,english:'主厨推荐'},
  {id:'staple',name:'主食',count:5,english:'热乎主食'},
  {id:'dessert',name:'甜品',count:4,english:'甜蜜时刻'},
  {id:'drink',name:'饮品',count:3,english:'一口清爽'},
  {id:'soup',name:'汤品',count:2,english:'暖心好汤'}
];
const ITEMS = [
  {id:'spicy-beef',cat:'special',name:'香辣小炒牛肉',price:48,desc:'鲜嫩牛肉猛火快炒，辣香裹着锅气。',ingredients:'牛肉、青红椒、蒜、葱',allergen:'含大豆、小麦',tag:'人气招牌',ambient:'fire',action:'感受锅气',effect:'spark',options:[['辣度','微辣','中辣','重辣']]},
  {id:'garlic-shrimp',cat:'special',name:'蒜香铁板虾',price:58,desc:'蒜香酱汁浇上铁板，香气滋滋作响。',ingredients:'鲜虾、蒜、黄油、香草',allergen:'含甲壳类、乳制品',tag:'滋滋现做',ambient:'steam',action:'淋上蒜香汁',effect:'pour',options:[['口味','经典蒜香','加辣蒜香']]},
  {id:'kung-pao-chicken',cat:'special',name:'宫保鸡丁',price:38,desc:'酸甜微辣，鸡丁与花生粒粒分明。',ingredients:'鸡腿肉、花生、葱、干辣椒',allergen:'含花生、大豆、小麦',tag:'下饭推荐',ambient:'shine',action:'刚炒好啦',effect:'sprinkle',options:[['辣度','不辣','微辣','中辣']]},
  {id:'braised-pork',cat:'special',name:'秘制红烧肉',price:46,desc:'慢火焖得酥软，浓汁晶亮入味。',ingredients:'五花肉、冰糖、葱姜',allergen:'含大豆、小麦',tag:'慢火入味',ambient:'shine',action:'浇一勺浓汁',effect:'pour',options:[['份量','标准份','加量 +¥16']]},
  {id:'claypot-tofu',cat:'special',name:'砂锅豆腐',price:32,desc:'砂锅锁住鲜味，揭盖就是暖香。',ingredients:'嫩豆腐、香菇、青菜',allergen:'含大豆',tag:'暖心砂锅',ambient:'steam',action:'掀盖看看',effect:'reveal',options:[['口味','原味','微辣']]},
  {id:'greens',cat:'special',name:'蒜蓉清炒时蔬',price:26,desc:'脆嫩时蔬，恰到好处的蒜香。',ingredients:'时令蔬菜、蒜、植物油',allergen:'无常见过敏原',tag:'清爽时蔬',ambient:'steam',action:'撒上蒜香',effect:'sprinkle',options:[['口味','经典蒜香','少油少盐']]},
  {id:'egg-fried-rice',cat:'staple',name:'金黄蛋炒饭',price:22,desc:'米粒松散分明，蛋香炒得刚刚好。',ingredients:'米饭、鸡蛋、葱',allergen:'含鸡蛋、大豆',tag:'锅气满满',ambient:'steam',action:'翻一勺看看',effect:'lift',options:[['份量','标准份','加量 +¥6']]},
  {id:'beef-noodles',cat:'staple',name:'招牌牛肉面',price:28,desc:'醇厚汤底配软嫩牛肉，暖到心里。',ingredients:'牛肉、面条、青菜、香料',allergen:'含小麦、大豆',tag:'热汤现煮',ambient:'steam',action:'挑起一筷',effect:'lift',options:[['辣度','不辣','微辣','中辣']]},
  {id:'xiaolongbao',cat:'staple',name:'鲜肉小笼包',price:24,desc:'现蒸出笼，薄皮包着鲜甜汤汁。',ingredients:'猪肉、小麦面粉、姜',allergen:'含小麦、大豆',tag:'一笼 6 只',ambient:'steam',action:'打开蒸笼',effect:'reveal',options:[['份量','6 只','12 只 +¥20']]},
  {id:'cheese-baked-rice',cat:'staple',name:'芝士焗饭',price:32,desc:'金黄焗香，舀起一勺满满芝士。',ingredients:'米饭、芝士、鸡肉、蘑菇',allergen:'含乳制品',tag:'香浓拉丝',ambient:'shine',action:'拉丝看看',effect:'stretch',options:[['口味','鸡肉蘑菇','双倍芝士 +¥8']]},
  {id:'scallion-noodles',cat:'staple',name:'葱油拌面',price:18,desc:'热油激出葱香，简简单单很满足。',ingredients:'面条、香葱、酱油',allergen:'含小麦、大豆',tag:'经典家常',ambient:'shine',action:'拌一拌',effect:'stir',options:[['份量','标准份','加量 +¥5']]},
  {id:'lava-cake',cat:'dessert',name:'熔岩巧克力蛋糕',price:28,desc:'轻轻切开，浓郁巧克力缓缓流出。',ingredients:'巧克力、黄油、鸡蛋、面粉',allergen:'含鸡蛋、乳制品、小麦',tag:'甜品人气',ambient:'shine',action:'切开看看',effect:'cut',options:[['甜度','经典甜度','少甜']]},
  {id:'caramel-pudding',cat:'dessert',name:'焦糖布丁',price:22,desc:'细腻柔软，焦糖带来一点微苦回甘。',ingredients:'牛奶、鸡蛋、焦糖',allergen:'含鸡蛋、乳制品',tag:'柔滑细腻',ambient:'shine',action:'轻碰一下',effect:'jiggle',options:[['温度','冰镇','常温']]},
  {id:'mango-pomelo',cat:'dessert',name:'杨枝甘露',price:24,desc:'芒果、柚子与西米，清甜有层次。',ingredients:'芒果、柚子、西米、椰乳',allergen:'含椰子',tag:'果香清甜',ambient:'cool',action:'搅拌一下',effect:'stir',options:[['甜度','正常甜','少甜','不额外加糖']]},
  {id:'strawberry-waffle',cat:'dessert',name:'草莓华夫饼',price:26,desc:'现烤华夫饼配草莓，外酥内软。',ingredients:'草莓、小麦面粉、奶油、鸡蛋',allergen:'含鸡蛋、乳制品、小麦',tag:'现烤出炉',ambient:'shine',action:'撒点糖粉',effect:'snow',options:[['搭配','经典奶油','少奶油']]},
  {id:'lime-soda',cat:'drink',name:'青柠气泡水',price:18,desc:'青柠酸香与细密气泡，清爽解腻。',ingredients:'青柠、气泡水、冰块',allergen:'无常见过敏原',tag:'冰爽气泡',ambient:'bubble',action:'清爽一下',effect:'cool',options:[['冰量','正常冰','少冰','去冰'],['甜度','正常甜','少甜','不加糖']]},
  {id:'osmanthus-tea',cat:'drink',name:'桂花乌龙热茶',price:16,desc:'桂花轻香落入乌龙茶，温润回甘。',ingredients:'乌龙茶、桂花',allergen:'无常见过敏原',tag:'暖暖热茶',ambient:'steam',action:'注入热茶',effect:'pour',options:[['温度','热饮','温饮'],['甜度','无糖','微糖']]},
  {id:'coconut-latte',cat:'drink',name:'生椰冰拿铁',price:24,desc:'咖啡与椰乳交融，入口丝滑清凉。',ingredients:'浓缩咖啡、椰乳、冰块',allergen:'含椰子',tag:'咖啡时光',ambient:'cool',action:'融合一下',effect:'blend',options:[['冰量','正常冰','少冰'],['甜度','无糖','微糖']]},
  {id:'corn-rib-soup',cat:'soup',name:'玉米排骨汤',price:32,desc:'慢炖排骨和甜玉米，一碗暖汤。',ingredients:'排骨、玉米、胡萝卜',allergen:'无常见过敏原',tag:'慢炖好汤',ambient:'steam',action:'盛一勺看看',effect:'lift',options:[['份量','一人份','双人份 +¥22']]},
  {id:'mushroom-chicken-soup',cat:'soup',name:'菌菇鸡汤',price:36,desc:'菌菇鲜味融进鸡汤，揭盖满屋香。',ingredients:'鸡肉、香菇、白玉菇、姜',allergen:'无常见过敏原',tag:'揭盖即香',ambient:'steam',action:'揭开汤盖',effect:'reveal',options:[['份量','一人份','双人份 +¥24']]}
];
const itemMap=Object.fromEntries(ITEMS.map(x=>[x.id,x]));
const state={category:'special',query:'',cart:readJSON('reqi-cart',[]),party:Number(localStorage.getItem('reqi-party')||2),modal:null,detailId:null,detailSelections:[],detailQty:1,detailNote:'',editingKey:null,lastFocus:null,effectTimer:null};
const $=s=>document.querySelector(s);
function readJSON(key,fallback){try{return JSON.parse(localStorage.getItem(key))??fallback}catch{return fallback}}
function saveCart(){localStorage.setItem('reqi-cart',JSON.stringify(state.cart))}
function image(id){return './food/'+id+'.webp'}
function money(n){return '¥'+n.toFixed(0)}
function escapeHTML(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function defaultSelections(item){return item.options.map(group=>group[1])}
function optionExtra(label){const m=label.match(/\+¥(\d+)/);return m?Number(m[1]):0}
function unitPrice(item,selections){return item.price+selections.reduce((sum,s)=>sum+optionExtra(s),0)}
function cartKey(id,selections,note){return JSON.stringify([id,selections,note.trim()])}
function countCart(){return state.cart.reduce((s,x)=>s+x.qty,0)}
function totalCart(){return state.cart.reduce((s,x)=>s+unitPrice(itemMap[x.id],x.selections)*x.qty,0)}
function totalItem(id){return state.cart.filter(x=>x.id===id).reduce((s,x)=>s+x.qty,0)}
function ambientMarkup(item,detail=false){const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const kind=item.ambient;let parts='';if(kind==='steam'||kind==='fire')parts='<i class="steam"></i><i class="steam"></i><i class="steam"></i>';if(kind==='fire')parts+='<i class="spark"></i><i class="spark"></i><i class="spark"></i>';if(kind==='bubble')parts='<i class="bubble"></i><i class="bubble"></i><i class="bubble"></i>';if(kind==='shine'||kind==='cool')parts='<i class="shine"></i>';return `<div class="food-visual ${reduce?'low-motion':''}" data-ambient="${kind}">${parts}</div>`}
function renderCategories(){const active=state.query?'':state.category;$('#categories').innerHTML=CATEGORIES.map(cat=>`<button class="category-tab ${active===cat.id?'active':''}" type="button" data-category="${cat.id}" aria-current="${active===cat.id?'true':'false'}">${cat.name}<span class="category-count">${cat.count}</span></button>`).join('')}
function renderCards(){let list;if(state.query){const q=state.query.trim().toLowerCase();list=ITEMS.filter(x=>(x.name+x.desc+x.ingredients).toLowerCase().includes(q));$('#sectionTitle').textContent='搜索结果';$('#sectionEyebrow').textContent='搜索菜品'}else{list=ITEMS.filter(x=>x.cat===state.category);const cat=CATEGORIES.find(x=>x.id===state.category);$('#sectionTitle').textContent=cat.name;$('#sectionEyebrow').textContent=cat.english}$('#resultCount').textContent=`${list.length} 道美味`;$('#foodGrid').innerHTML=list.length?list.map(item=>`<article class="food-card" data-card-id="${item.id}"><button type="button" class="card-image-button" data-open="${item.id}" aria-label="查看${item.name}详情"><img src="${image(item.id)}" alt="${item.name}" loading="lazy">${ambientMarkup(item)}<span class="card-tag">${item.tag}</span></button><div class="card-content"><button type="button" class="card-title" data-open="${item.id}">${item.name}</button><p class="card-desc">${item.desc}</p><div class="card-footer"><span class="price"><small>¥</small>${item.price}</span>${totalItem(item.id)?`<div class="stepper"><button type="button" data-quick-minus="${item.id}" aria-label="减少一份${item.name}">−</button><b>${totalItem(item.id)}</b><button type="button" data-quick-plus="${item.id}" aria-label="增加一份${item.name}">+</button></div>`:`<button class="card-add" type="button" data-quick-plus="${item.id}" aria-label="加入一份${item.name}">+</button>`}</div></div></article>`).join(''):`<div class="empty-result"><b>还没有找到这道菜</b><p>试试输入菜名或食材。</p><button type="button" id="resetSearch">查看全部菜品</button></div>`;observeAmbient()}
function orderRows(limit=4){return state.cart.slice(0,limit).map(line=>{const item=itemMap[line.id];return `<div class="order-row"><img src="${image(item.id)}" alt="" loading="lazy"><div><b>${item.name}</b><small>${line.selections.join(' · ')} × ${line.qty}</small></div><span class="line-price">${money(unitPrice(item,line.selections)*line.qty)}</span></div>`}).join('')}
function renderOrders(){
  const count=countCart(),total=totalCart(),last=readJSON('reqi-last-order',null);
  $('#partyDisplay').textContent=state.party;
  $('#desktopOrder').innerHTML=`<div class="order-head"><div><h2>我的订单</h2><small>A06 桌 · ${state.party} 位用餐</small></div><div class="order-title-icon" aria-hidden="true">⌑</div></div>${count?`${orderRows()}${state.cart.length>4?`<p class="more-items">还有 ${state.cart.length-4} 种美味</p>`:''}<div class="order-summary"><span>共 ${count} 份</span><b>${money(total)}</b></div><button class="primary-button order-checkout" type="button" data-cart>查看订单 · 去下单</button>`:`<div class="order-empty"><div class="order-empty-icon" aria-hidden="true">♨</div><b>还没选好吃的</b><p>从菜单里挑几道喜欢的吧</p></div><button class="primary-button order-checkout" type="button" ${last?'data-last-order':'disabled'}>${last?'查看最近订单':'去下单'}</button>`}${count&&last?'<button class="recent-order-link" type="button" data-last-order>查看最近订单</button>':''}`;
  $('#mobileOrderBar').innerHTML=`<div class="mobile-order-inner"><div class="cart-bag" aria-hidden="true">♨${count?`<i>${count}</i>`:''}</div><div class="mobile-order-info"><strong>${money(total)}</strong><small>${count?`已选 ${count} 份美味`:'还没选好吃的'}</small></div><button class="primary-button" type="button" ${count?'data-cart':last?'data-last-order':'disabled'}>${count?'查看订单':last?'最近订单':'查看订单'}</button></div>`;
}
function renderAll(){renderCategories();renderCards();renderOrders()}
function toast(message){const el=$('#toast');el.textContent=message;el.classList.add('visible');clearTimeout(toast.timer);toast.timer=setTimeout(()=>el.classList.remove('visible'),2300)}
function addLine(item,selections,qty,note=''){const key=cartKey(item.id,selections,note);const found=state.cart.find(x=>x.key===key);if(found)found.qty+=qty;else state.cart.push({key,id:item.id,selections:[...selections],note:note.trim(),qty});saveCart();renderCards();renderOrders();toast(`已加入 ${item.name}`)}
function quickPlus(id){const item=itemMap[id];const existing=state.cart.find(x=>x.id===id&&x.note===''&&JSON.stringify(x.selections)===JSON.stringify(defaultSelections(item)));if(existing)existing.qty++;else state.cart.push({key:cartKey(id,defaultSelections(item),''),id,selections:defaultSelections(item),note:'',qty:1});saveCart();renderCards();renderOrders();toast(`已加入 ${item.name}`)}
function quickMinus(id){const lines=state.cart.filter(x=>x.id===id);if(!lines.length)return;const line=lines[lines.length-1];line.qty--;if(line.qty<=0)state.cart=state.cart.filter(x=>x!==line);saveCart();renderCards();renderOrders()}
function openModal(html,kind){state.lastFocus=document.activeElement;state.modal=kind;$('#overlayRoot').innerHTML=`<div class="overlay" data-overlay><div class="sheet ${kind}-sheet" role="dialog" aria-modal="true" aria-label="${kind==='detail'?'菜品详情':kind==='cart'?'我的订单':kind==='success'?'下单成功':'用餐信息'}">${html}</div></div>`;document.body.classList.add('modal-open');const close=$('#overlayRoot .close-button');if(close)close.focus();else $('#overlayRoot button')?.focus()}
function closeModal(){clearTimeout(state.effectTimer);state.modal=null;state.detailId=null;$('#overlayRoot').innerHTML='';document.body.classList.remove('modal-open');state.lastFocus?.focus?.()}
function detailHTML(item){const cat=CATEGORIES.find(x=>x.id===item.cat);const options=item.options.map((g,gi)=>`<div><p class="detail-label">${g[0]}</p><div class="option-group" role="group" aria-label="${g[0]}">${g.slice(1).map((choice,ci)=>`<button class="option ${state.detailSelections[gi]===choice?'active':''}" type="button" data-option-group="${gi}" data-option-index="${ci}" aria-pressed="${state.detailSelections[gi]===choice}">${choice}</button>`).join('')}</div></div>`).join('');return `<div class="detail-layout"><div class="detail-photo-wrap" id="detailPhoto"><img src="${image(item.id)}" alt="${item.name}">${["lift","stir","jiggle","stretch","cut","blend"].includes(item.effect)?`<img class="food-motion-patch" src="${image(item.id)}" alt="" aria-hidden="true">`:""}${item.effect==="reveal"?`<img class="reveal-lid" src="./food/${item.id==="xiaolongbao"?"bamboo-steamer-lid":"claypot-lid"}.webp" alt="" aria-hidden="true">`:""}${ambientMarkup(item,true)}<div class="detail-effect" id="detailEffect"><div class="effect-glow"></div><div class="effect-wave"></div><div class="effect-stream"></div><div class="effect-particles"></div></div><span class="detail-photo-label">${item.tag} · 现点现做</span></div><div class="detail-content"><span class="detail-category">${cat.name} / ${item.tag}</span><h2>${item.name}</h2><p class="detail-description">${item.desc}</p><div class="detail-price"><small>¥</small><span id="detailPrice">${unitPrice(item,state.detailSelections)}</span></div><div class="detail-line"></div>${options}<p class="detail-ingredients">主要食材：${item.ingredients}<br>过敏原提示：${item.allergen}</p><div class="interaction-row"><button class="interaction-button" type="button" id="playEffect">${item.action} ↗</button><span class="interaction-hint">点击体验菜品动态</span></div><label class="detail-label" for="detailNote">口味备注</label><textarea id="detailNote" class="note-input" placeholder="例如：不要香菜（选填）" maxlength="100">${escapeHTML(state.detailNote)}</textarea><div class="detail-actions"><div class="stepper"><button type="button" data-detail-minus aria-label="减少份数">−</button><b id="detailQty">${state.detailQty}</b><button type="button" data-detail-plus aria-label="增加份数">+</button></div><button class="primary-button" id="addDetail" type="button">${state.editingKey?'保存修改':'加入订单'} · <span id="detailTotal">${money(unitPrice(item,state.detailSelections)*state.detailQty)}</span></button></div></div></div><button class="close-button" data-close type="button" aria-label="关闭详情">×</button>`}
function openDetail(id,editingKey=null){const item=itemMap[id];if(!item)return;state.detailId=id;state.editingKey=editingKey;const existing=editingKey?state.cart.find(x=>x.key===editingKey):null;state.detailSelections=existing?[...existing.selections]:defaultSelections(item);state.detailQty=existing?.qty||1;state.detailNote=existing?.note||'';openModal(detailHTML(item),'detail')}
function updateDetailNumbers(){const item=itemMap[state.detailId];$('#detailPrice').textContent=unitPrice(item,state.detailSelections);$('#detailTotal').textContent=money(unitPrice(item,state.detailSelections)*state.detailQty);$('#detailQty').textContent=state.detailQty;document.querySelectorAll('#overlayRoot .option').forEach(el=>{const g=Number(el.dataset.optionGroup),i=Number(el.dataset.optionIndex),sel=item.options[g][i+1]===state.detailSelections[g];el.classList.toggle('active',sel);el.setAttribute('aria-pressed',sel)})}
function playEffect(){if(matchMedia('(prefers-reduced-motion: reduce)').matches){toast('已开启减少动态效果');return}const item=itemMap[state.detailId],photo=$('#detailPhoto'),layer=$('#detailEffect .effect-particles');if(!photo)return;photo.className='detail-photo-wrap';void photo.offsetWidth;photo.classList.add('effect-playing','effect-'+item.effect);let color=item.effect==='spark'?'#ff9a39':item.effect==='snow'?'#fff9e7':item.effect==='cool'?'#ddf8ef':item.effect==='pour'?'#bb7137':'#ffe7a0';let particles=Array.from({length:item.effect==='snow'?23:12},(_,i)=>{const x=27+(i*31%48),y=item.effect==='snow'?8+(i*7%23):32+(i*11%34),dx=(i*17%90-45)+'px',dy=(item.effect==='snow'?110:55+(i*13%70))+'px';return `<span style="--x:${x}%;--y:${y}%;--s:${2+i%4}px;--d:${(i%7)*.08}s;--dx:${dx};--dy:${dy};--c:${color}"></span>`}).join('');layer.innerHTML=particles;$('#playEffect').textContent='再看一次 ↗';clearTimeout(state.effectTimer);state.effectTimer=setTimeout(()=>{photo.classList.remove('effect-playing');if(item.effect==='reveal')photo.classList.add('effect-revealed');layer.innerHTML=''},2200)}
function cartHTML(){const count=countCart();return `<button class="close-button" data-close type="button" aria-label="关闭订单">×</button><h2>我的订单</h2><p class="sheet-subtitle">A06 桌 · 现点现做 · ${count} 份</p>${count?`<div class="cart-list">${state.cart.map(line=>{const item=itemMap[line.id];return `<div class="cart-item"><img src="${image(item.id)}" alt=""><div><p class="cart-item-name">${item.name}</p><p class="cart-item-meta">${line.selections.join(' · ')}${line.note?`<br>备注：${escapeHTML(line.note)}`:''}</p><button class="cart-item-edit" type="button" data-edit="${escapeHTML(line.key)}">修改口味</button></div><div class="cart-item-right"><div class="cart-item-price">${money(unitPrice(item,line.selections)*line.qty)}</div><div class="stepper"><button type="button" data-cart-minus="${escapeHTML(line.key)}" aria-label="减少${item.name}">−</button><b>${line.qty}</b><button type="button" data-cart-plus="${escapeHTML(line.key)}" aria-label="增加${item.name}">+</button></div></div></div>`}).join('')}</div><div class="cart-total"><span>合计 · ${count} 份</span><strong>${money(totalCart())}</strong></div><div class="cart-form"><label><span class="field-label">桌号</span><input class="field-box" value="A06" readonly aria-label="桌号 A06"></label><label><span class="field-label">用餐人数</span><input class="field-box" id="cartParty" type="number" min="1" max="20" value="${state.party}"></label></div><label><span class="field-label" style="margin-top:14px">整单备注</span><input class="field-box" id="orderNote" maxlength="150" placeholder="有需要请告诉我们（选填）"></label><button class="primary-button" id="submitOrder" type="button">确认下单 · ${money(totalCart())}</button>`:`<div class="cart-empty"><b>购物车还是空的</b><p>选几道喜欢的菜，热乎乎地上桌。</p><button type="button" data-close>返回点单</button></div>`}`}
function openCart(){openModal(cartHTML(),'cart')}
function updateCartLine(key,delta){const line=state.cart.find(x=>x.key===key);if(!line)return;line.qty+=delta;if(line.qty<=0)state.cart=state.cart.filter(x=>x!==line);saveCart();renderCards();renderOrders();const orderNote=$('#orderNote')?.value||'';const party=$('#cartParty')?.value||state.party;if(state.modal==='cart'){$('#overlayRoot .sheet').innerHTML=cartHTML();if($('#orderNote'))$('#orderNote').value=orderNote;if($('#cartParty'))$('#cartParty').value=party}}
function orderResultHTML(order){
  return `<button class="close-button" data-close type="button" aria-label="关闭">×</button><div class="success-icon" aria-hidden="true">✓</div><h2>演示订单已生成</h2><p>订单保存在这台设备上，不会发送到餐厅厨房。</p><div class="success-detail"><div><span>订单编号</span><b>${order.number}</b></div><div><span>用餐位置</span><b>${order.table} 桌 · ${order.party} 位</b></div><div><span>订单状态</span><b>${order.status}</b></div><div class="order-result-items">${order.items.map(line=>{const item=itemMap[line.id];return `<div><span>${item.name} · ${line.selections.join(' / ')} × ${line.qty}</span><b>${money(unitPrice(item,line.selections)*line.qty)}</b></div>`}).join('')}</div>${order.note?`<div><span>整单备注</span><b>${escapeHTML(order.note)}</b></div>`:''}<div class="success-total"><span>合计</span><b>${money(order.total)}</b></div></div><button class="primary-button" data-close type="button">继续点餐</button>`;
}
function openLastOrder(){const order=readJSON('reqi-last-order',null);if(order?.items?.length)openModal(orderResultHTML(order),'success')}
function submitOrder(){
  if(!state.cart.length)return;
  const party=Number($('#cartParty')?.value||state.party);
  if(!Number.isInteger(party)||party<1||party>20){toast('用餐人数请填写 1 到 20');$('#cartParty')?.focus();return}
  state.party=party;localStorage.setItem('reqi-party',String(party));
  const note=$('#orderNote')?.value.trim()||'';
  const order={number:'Y'+Date.now().toString().slice(-8),date:new Date().toISOString(),table:'A06',party,note,items:structuredClone(state.cart),total:totalCart(),status:'已记录 · 演示订单'};
  localStorage.setItem('reqi-last-order',JSON.stringify(order));state.cart=[];saveCart();renderAll();openModal(orderResultHTML(order),'success');
}
function openTable(){openModal(`<button class="close-button" data-close type="button" aria-label="关闭">×</button><h2>用餐信息</h2><p>欢迎来到热气食堂，当前为堂食演示点单。</p><div class="party-control"><span>桌号</span><strong>A06</strong></div><div class="party-control"><span>用餐人数</span><div class="stepper"><button type="button" data-party-minus aria-label="减少用餐人数">−</button><b id="partyNumber">${state.party}</b><button type="button" data-party-plus aria-label="增加用餐人数">+</button></div></div><button class="primary-button" data-close type="button" style="width:100%">确定</button>`,'table')}
function observeAmbient(){if(!('IntersectionObserver'in window))return;window.cardObserver?.disconnect();window.cardObserver=new IntersectionObserver(entries=>entries.forEach(e=>e.target.querySelector('.food-visual')?.classList.toggle('paused',!e.isIntersecting)),{rootMargin:'100px'});document.querySelectorAll('.food-card').forEach(card=>window.cardObserver.observe(card))}
function attachEvents(){document.addEventListener('click',e=>{const t=e.target;const category=t.closest('[data-category]');if(category){state.category=category.dataset.category;state.query='';$('#searchInput').value='';$('#clearSearch').classList.remove('visible');renderAll();return}const open=t.closest('[data-open]');if(open){openDetail(open.dataset.open);return}const plus=t.closest('[data-quick-plus]');if(plus){quickPlus(plus.dataset.quickPlus);return}const minus=t.closest('[data-quick-minus]');if(minus){quickMinus(minus.dataset.quickMinus);return}if(t.closest('[data-cart]')){openCart();return}if(t.closest('[data-last-order]')){openLastOrder();return}if(t.closest('[data-close]')){closeModal();return}if(t.closest('[data-overlay]')&&t===t.closest('[data-overlay]')){closeModal();return}if(t.closest('[data-detail-plus]')){state.detailQty=Math.min(20,state.detailQty+1);updateDetailNumbers();return}if(t.closest('[data-detail-minus]')){state.detailQty=Math.max(1,state.detailQty-1);updateDetailNumbers();return}const opt=t.closest('[data-option-group]');if(opt){const g=Number(opt.dataset.optionGroup),i=Number(opt.dataset.optionIndex),item=itemMap[state.detailId];state.detailSelections[g]=item.options[g][i+1];updateDetailNumbers();return}if(t.id==='playEffect'){playEffect();return}if(t.id==='addDetail'){const item=itemMap[state.detailId],note=$('#detailNote').value;const edit=state.editingKey;if(edit)state.cart=state.cart.filter(x=>x.key!==edit);addLine(item,state.detailSelections,state.detailQty,note);closeModal();return}const cplus=t.closest('[data-cart-plus]');if(cplus){updateCartLine(cplus.dataset.cartPlus,1);return}const cminus=t.closest('[data-cart-minus]');if(cminus){updateCartLine(cminus.dataset.cartMinus,-1);return}const edit=t.closest('[data-edit]');if(edit){const line=state.cart.find(x=>x.key===edit.dataset.edit);if(line)openDetail(line.id,line.key);return}if(t.id==='submitOrder'){submitOrder();return}if(t.id==='tableButton'||t.closest('#tableButton')){openTable();return}if(t.closest('[data-party-plus]')){state.party=Math.min(20,state.party+1);$('#partyNumber').textContent=state.party;localStorage.setItem('reqi-party',state.party);renderOrders();return}if(t.closest('[data-party-minus]')){state.party=Math.max(1,state.party-1);$('#partyNumber').textContent=state.party;localStorage.setItem('reqi-party',state.party);renderOrders();return}if(t.id==='heroButton'){openDetail('spicy-beef');return}if(t.id==='resetSearch'){state.query='';$('#searchInput').value='';$('#clearSearch').classList.remove('visible');renderAll();return}if(t.id==='clearSearch'){state.query='';$('#searchInput').value='';$('#clearSearch').classList.remove('visible');renderAll();$('#searchInput').focus();return}});$('#searchInput').addEventListener('input',e=>{state.query=e.target.value.trim();$('#clearSearch').classList.toggle('visible',!!state.query);renderCategories();renderCards()});document.addEventListener('keydown',e=>{if(!state.modal)return;if(e.key==='Escape'){closeModal();return}if(e.key==='Tab'){const focusable=[...document.querySelectorAll('#overlayRoot .sheet button:not([disabled]),#overlayRoot .sheet input:not([disabled]),#overlayRoot .sheet textarea:not([disabled]),#overlayRoot .sheet a[href]')].filter(el=>el.offsetParent!==null);if(!focusable.length)return;const first=focusable[0],last=focusable[focusable.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}}})}
function init(){state.cart=state.cart.filter(x=>itemMap[x.id]&&Number.isInteger(x.qty)&&x.qty>0&&Array.isArray(x.selections));$('#heroPhoto').style.backgroundImage=`url("${image('spicy-beef')}")`;renderAll();attachEvents()}
init();
function registerWebTools(){
  const context=document.modelContext;
  if(!context?.registerTool)return;
  const tools=[
    {name:'read_menu',title:'查看菜单',description:'读取热气食堂的中文菜单、分类、价格与菜品 ID。',inputSchema:{type:'object',properties:{category:{type:'string',enum:CATEGORIES.map(c=>c.id)}},additionalProperties:false},annotations:{readOnlyHint:true},execute(input){const cat=input?.category;if(cat&&!CATEGORIES.some(c=>c.id===cat))throw Error('无效分类');return ITEMS.filter(x=>!cat||x.cat===cat).map(x=>({id:x.id,name:x.name,category:x.cat,price:x.price,description:x.desc}))}},
    {name:'add_dish_to_order',title:'加入菜品',description:'将指定菜品加入当前设备的演示订单，可指定份数。',inputSchema:{type:'object',properties:{id:{type:'string',enum:ITEMS.map(x=>x.id)},quantity:{type:'integer',minimum:1,maximum:20}},required:['id','quantity'],additionalProperties:false},annotations:{readOnlyHint:false},execute(input){const item=itemMap[input?.id];if(!item||!Number.isInteger(input?.quantity)||input.quantity<1||input.quantity>20)throw Error('无效菜品或份数');addLine(item,defaultSelections(item),input.quantity);return {item:item.name,added:input.quantity,orderCount:countCart(),total:totalCart()}}},
    {name:'read_current_order',title:'查看当前订单',description:'读取当前设备购物车中的菜品、份数和金额。',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:true},execute(){return {table:'A06',party:state.party,items:state.cart.map(x=>({name:itemMap[x.id].name,options:x.selections,quantity:x.qty,subtotal:unitPrice(itemMap[x.id],x.selections)*x.qty})),count:countCart(),total:totalCart()}}}
  ];
  for(const tool of tools){try{Promise.resolve(context.registerTool(tool)).catch(()=>{})}catch{}}
}
registerWebTools();
